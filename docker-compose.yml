services:
  db:
    image: 'postgres:17'
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - '6432:5432'
    volumes: 
      - /var/lib/postgresql/data
