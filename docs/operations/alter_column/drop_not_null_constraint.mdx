---
title: Drop not null constraint
description: Drop not null operations drop a `NOT NULL` constraint from a column.
---

## Structure

```json
{
  "alter_column": {
    "table": "table name",
    "column": "column name",
    "nullable": true,
    "up": "SQL expression",
    "down": "SQL expression"
  }
}
```

## Examples

### Remove `NOT NULL` from a column

Remove `NOT NULL` from the `title` column in the `posts` table:

<ExampleSnippet example="31_unset_not_null.yaml" languange="yaml" />
