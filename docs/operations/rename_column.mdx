---
title: Rename column
description: A rename column operation renames a column.
---

## Structure

```json
{
  "rename_column": {
    "table": "table name",
    "column": "old column name",
    "name": "new column name"
  }
}
```

In the new schema version, the column will have its new name. In the old schema version the column still has its old name.

## Examples

### Rename a column

Rename the `role` column in the `employees` table:

<ExampleSnippet example="13_rename_column.yaml" languange="yaml" />
