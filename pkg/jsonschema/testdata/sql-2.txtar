This is a valid 'sql' migration.
It specifies `up` but not `down`.

-- create_table.json --
{
  "name": "migration_name",
  "operations": [
    {
      "sql": {
        "up": "CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT)"
      }
    }
  ]
}

-- valid --
true
