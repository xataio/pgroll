This is a valid 'sql' migration.
It specifies both `up` and `down`.

-- create_table.json --
{
  "name": "migration_name",
  "operations": [
    {
      "sql": {
        "up": "CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT)",
        "down": "DROP TABLE users"
      }
    }
  ]
}

-- valid --
true
